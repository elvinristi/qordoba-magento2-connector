version: "2"

options:
  verbose: true
  max-attempt: 30

syncs:
  magento-sources-sync:
     src: './src/'
     sync_user: 'www-data'
     sync_userid: '1000'
     sync_group: 'www-data'
     sync_host_ip: 'auto'
     sync_host_port: 10872
     sync_strategy: 'rsync'